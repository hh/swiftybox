diff --git a/shell/ash.c b/shell/ash.c
index bbd730770..f8e9a1234 100644
--- a/shell/ash.c
+++ b/shell/ash.c
@@ -200,6 +200,11 @@
 #include <fnmatch.h>
 #include <sys/times.h>
 #include <sys/utsname.h> /* for setting $HOSTNAME */
+
+/* SwiftyλBox integration */
+extern int swiftybox_builtin_wrapper(int argc, char **argv);
+extern int is_swiftybox_command(const char *name);
+/* End SwiftyλBox */
+
 #include "busybox.h" /* for applet_names */
 #if ENABLE_FEATURE_SH_EMBEDDED_SCRIPTS
 # include "embedded_scripts.h"
@@ -10684,8 +10689,10 @@ evalbltin(const struct builtincmd *cmd, int argc, char **argv, int flags)
 	commandname = argv[0];
 	argptr = argv + 1;
 	optptr = NULL;                  /* initialize nextopt */
 	if (cmd == EVALCMD)
 		status = evalcmd(argc, argv, flags);
+	else if (is_swiftybox_command(argv[0]))
+		status = swiftybox_builtin_wrapper(argc, argv);
 	else
 		status = (*cmd->builtin)(argc, argv);
 	flush_stdout_stderr();
